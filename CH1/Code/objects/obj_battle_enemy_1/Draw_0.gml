draw_self()
if not dying and not sparing
	draw_sprite_ext(sprite_index,image_index,x,y,image_xscale,image_yscale,image_angle,c_lime,green)
	
if (point_distance(x, y, obj_player.x, obj_player.y) <= (obj_player.sprite_width * 1.2))
{
	
}
else
{
}